# OpenAD Zero v0.1.0-rc1

## Summary

OpenAD Zero v0.1.0-rc1 is the first release candidate of the safe-by-default Active Directory lab operations platform.

It provides mission creation, internal scope validation, Nmap discovery, NetExec SMB safe enumeration, Nuclei safe web exposure scanning, BloodHound / SharpHound evidence upload, BloodHound Explorer V1, Evidence Manager, Reporting Engine, Lab Operations, Timeline, Progress Score, Docker Compose deployment and CI quality checks.

## Who is this for?

- Cybersecurity students.
- Internal lab operators.
- CTF and training environments.
- Authorized internal assessment workflows.
- Demonstrations of safe-by-design AD assessment orchestration.

## What is included?

- Backend API.
- Frontend cockpit.
- Worker queue.
- Evidence workflow.
- Reporting workflow.
- Lab operations workflow.
- Optional BloodHound CE profile.
- QA and CI checks.

## What is intentionally not included?

- No automatic exploitation.
- No credential dumping.
- No LSASS dump.
- No DCSync.
- No pass-the-hash.
- No persistence.
- No EDR bypass.
- No lateral movement automation.
- No arbitrary shell command from the frontend.

## Upgrade / install

```bash
cp .env.example .env
make up-build
make migrate
make smoke
```

## Validation

```bash
make backend-test
make frontend-build
make security-check
make release-check
```

## Known limitations

- BloodHound CE integration requires external configuration.
- PDF reporting is not included.
- Multi-user authentication is not included.
- This release candidate is intended for controlled and authorized environments.

## Tool execution model

OpenAD-Zero now distinguishes documented tools from runnable tools with the `executable_after_human_approval` integration status. Advanced AD/Pentest workflows such as Kerbrute, gMSADumper, DonPAPI, Coercer, BloodyAD, controlled Impacket workflows and Responder analyze mode are usable only through declared templates and explicit operator gates.

An OpenAD-Zero tool is executable only when:
1. the tool is declared in `tools.yml`;
2. the selected template is declared in `command_templates.py`;
3. the selected template is referenced by the tool;
4. the target is inside the validated scope;
5. the command preview has been generated;
6. human approval is confirmed;
7. explicit terms are accepted.

The frontend never sends a raw command to execute. The backend always rebuilds argv from an allowlisted template, refuses out-of-scope targets, refuses `0.0.0.0/0` and `::/0`, refuses public IPs by default, and keeps `manual_only`, `blocked_auto` and `planned` tools non-runnable. The GUI provides a dedicated landscape console per tool, separated terminal output and history, and a collapsible left sidebar grouped by Scope & Setup, Recon, SMB / NetExec, Active Directory, Coercion / Capture, Impacket, Credentials Review, Reports and Settings.

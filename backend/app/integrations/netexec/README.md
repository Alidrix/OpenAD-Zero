# NetExec

V2 safe SMB enum integration. Only approved backend templates are allowed: fingerprint, SMB signing check, null session check, and anonymous share listing. Offensive modules, dumping, exec, spraying, brute force and spidering are blocked by command policy.

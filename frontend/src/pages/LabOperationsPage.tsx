import {useEffect,useState} from 'react';import {Link,useParams} from 'react-router-dom';import {getOperationsSummary,syncOperations} from '../lib/api';import type {OperationsSummary} from '../types/operations';import {ObjectiveCard} from '../components/operations/ObjectiveCard';import {ProgressScoreCard} from '../components/operations/ProgressScoreCard';import {MissionPhasesList} from '../components/operations/MissionPhasesList';import {TimelineList} from '../components/operations/TimelineList';import {OperationsSummaryCards} from '../components/operations/OperationsSummaryCards';
export function LabOperationsPage(){const {missionId}=useParams();const [data,setData]=useState<OperationsSummary|null>(null);const load=()=>{if(missionId)getOperationsSummary(missionId).then(setData)};useEffect(()=>{load()},[missionId]);if(!missionId)return null;if(!data)return <div>Loading operations...</div>;return <div className="space-y-5"><div className="flex items-center justify-between"><h1 className="text-2xl font-bold">Lab Operations</h1><button className="rounded bg-blue-600 px-3 py-2 text-white" onClick={()=>syncOperations(missionId).then(()=>load())}>Sync operations</button></div><ObjectiveCard objective={data.objective}/><ProgressScoreCard progress={data.progress}/><MissionPhasesList phases={data.phases}/><TimelineList events={data.recent_timeline}/><OperationsSummaryCards counts={data.counts}/><section className="rounded-2xl border bg-white p-5 dark:border-slate-800 dark:bg-slate-900"><h2 className="text-lg font-semibold">Next Steps</h2><div className="mt-3 flex gap-3"><Link to={`/missions/${missionId}/report`}>Report</Link><Link to={`/missions/${missionId}/evidence`}>Evidence</Link><Link to={`/missions/${missionId}/bloodhound/explorer`}>BloodHound Explorer</Link><Link to={`/missions/${missionId}/timeline`}>Full Timeline</Link></div></section></div>}

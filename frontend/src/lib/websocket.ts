export function missionSocket(id:string,opts?:{replay?:boolean;after_id?:string}){const base=(import.meta.env.VITE_WS_URL as string)||`${location.protocol==='https:'?'wss':'ws'}://${location.host}`;const qs=new URLSearchParams();if(opts?.replay)qs.set('replay','true');if(opts?.after_id)qs.set('after_id',opts.after_id);return new WebSocket(`${base}/ws/missions/${id}${qs.toString()?`?${qs}`:''}`)}

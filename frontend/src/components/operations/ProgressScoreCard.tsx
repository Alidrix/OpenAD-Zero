import type {ProgressScore} from '../../types/operations';
export function ProgressScoreCard({progress}:{progress:ProgressScore}){return <section className="rounded-2xl border bg-white p-5 dark:border-slate-800 dark:bg-slate-900"><h2 className="text-lg font-semibold">Progress Score</h2><div className="mt-3 h-3 rounded bg-slate-200 dark:bg-slate-800"><div className="h-3 rounded bg-blue-500" style={{width:`${progress.score}%`}}/></div><div className="mt-2 text-2xl font-bold">{progress.score}% <span className="text-sm font-normal text-slate-500">{progress.level}</span></div><p className="mt-2 text-sm text-green-600">Completed: {progress.completed_items.join(', ')||'None'}</p><p className="text-sm text-amber-600">Missing: {progress.missing_items.join(', ')||'None'}</p></section>}

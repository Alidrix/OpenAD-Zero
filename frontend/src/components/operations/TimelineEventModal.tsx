import {useState} from 'react';
export function TimelineEventModal({onClose,onSubmit}:{onClose:()=>void;onSubmit:(p:{title:string;description?:string;source:string;severity:string;event_type:string})=>void}){const [title,setTitle]=useState('');const [description,setDescription]=useState('');const [severity,setSeverity]=useState('info');return <div className="fixed inset-0 grid place-items-center bg-black/40"><form className="w-full max-w-lg rounded-2xl bg-white p-5 dark:bg-slate-900" onSubmit={e=>{e.preventDefault();onSubmit({title,description,source:'manual',severity,event_type:'manual.event'})}}><h2 className="text-lg font-semibold">Add manual event</h2><input className="mt-3 w-full rounded border p-2 dark:bg-slate-800" placeholder="Title" value={title} onChange={e=>setTitle(e.target.value)} required/><textarea className="mt-3 w-full rounded border p-2 dark:bg-slate-800" placeholder="Description" value={description} onChange={e=>setDescription(e.target.value)}/><select className="mt-3 w-full rounded border p-2 dark:bg-slate-800" value={severity} onChange={e=>setSeverity(e.target.value)}>{['info','low','medium','high','critical','success','warning','error'].map(s=><option key={s}>{s}</option>)}</select><div className="mt-4 flex justify-end gap-2"><button type="button" onClick={onClose}>Cancel</button><button className="rounded bg-blue-600 px-3 py-2 text-white">Add</button></div></form></div>}

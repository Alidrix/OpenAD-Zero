import type {MissionObjective} from '../../types/operations';
export function ObjectiveCard({objective}:{objective:MissionObjective}){return <section className="rounded-2xl border bg-white p-5 dark:border-slate-800 dark:bg-slate-900"><h2 className="text-lg font-semibold">Mission Objective</h2><p className="mt-2 text-xl">{objective.objective_name}</p><p className="text-sm text-slate-500">Type: {objective.objective_type} · Target: {objective.objective_target||'n/a'} · Status: {objective.objective_status}</p>{objective.operator_note&&<p className="mt-2 text-sm">{objective.operator_note}</p>}</section>}

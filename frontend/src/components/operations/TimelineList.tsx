import type {TimelineEvent} from '../../types/operations';
export function TimelineList({events}:{events:TimelineEvent[]}){return <section className="rounded-2xl border bg-white p-5 dark:border-slate-800 dark:bg-slate-900"><h2 className="text-lg font-semibold">Recent Timeline</h2><div className="mt-3 space-y-2">{events.map(e=><div key={e.id} className="rounded-xl bg-slate-50 p-3 dark:bg-slate-800"><div className="flex justify-between"><b>{e.title}</b><span className="text-xs">{e.source} · {e.severity}</span></div><p className="text-xs text-slate-500">{new Date(e.created_at).toLocaleString()}</p>{e.description&&<p className="text-sm">{e.description}</p>}</div>)}</div></section>}

import type {MissionPhase} from '../../types/operations';
export function MissionPhasesList({phases}:{phases:MissionPhase[]}){return <section className="rounded-2xl border bg-white p-5 dark:border-slate-800 dark:bg-slate-900"><h2 className="text-lg font-semibold">Mission Phases</h2><div className="mt-3 space-y-2">{phases.map(p=><div key={p.id} className="flex justify-between rounded-xl bg-slate-50 p-3 dark:bg-slate-800"><div><b>{p.order_index}. {p.name}</b><p className="text-sm text-slate-500">{p.description}</p>{p.summary&&<p className="text-sm">{p.summary}</p>}</div><span className="h-fit rounded-full bg-blue-100 px-2 py-1 text-xs text-blue-700 dark:bg-blue-950 dark:text-blue-200">{p.status}</span></div>)}</div></section>}

import type {EvidencePreview as P} from '../../types/evidence';
export function EvidencePreview({preview}:{preview?:P|null}){if(!preview)return null;if(!preview.available)return <div className="rounded bg-slate-100 p-3 dark:bg-slate-800">Preview not available</div>;let content=preview.content;if(preview.format==='json'){try{content=JSON.stringify(JSON.parse(content),null,2)}catch{}}return <div><div className="mb-2 text-sm text-slate-500">Format: {preview.format}{preview.truncated?' · Preview truncated':''}</div><pre className="max-h-96 overflow-auto rounded bg-slate-950 p-4 text-xs text-slate-100">{content}</pre></div>}

import type {Evidence} from '../../types/evidence';import type {EvidencePreview as P} from '../../types/evidence';import {EvidencePreview} from './EvidencePreview';import {EvidenceLinksPanel} from './EvidenceLinksPanel';
export function EvidenceDetailPanel({missionId,evidence,preview}:{missionId:string;evidence?:Evidence|null;preview?:P|null}){if(!evidence)return <div className="rounded-xl border p-4 text-slate-500">Select evidence to view details.</div>;return <div className="space-y-4 rounded-xl border p-4"><h2 className="text-xl font-bold">{evidence.label}</h2><p>{evidence.description}</p><div className="grid gap-2 text-sm"><div><b>Filename:</b> {evidence.filename}</div><div><b>SHA256:</b> <span className="break-all font-mono">{evidence.sha256}</span></div><div><b>Stored path:</b> {evidence.stored_path}</div></div><div><h3 className="font-semibold">Metadata</h3><pre className="max-h-60 overflow-auto rounded bg-slate-100 p-3 text-xs dark:bg-slate-800">{JSON.stringify(evidence.metadata_json,null,2)}</pre></div><EvidencePreview preview={preview}/><EvidenceLinksPanel missionId={missionId} evidence={evidence}/></div>}

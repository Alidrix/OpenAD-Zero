export function FindingCard({finding}:{finding:any}){return <div className='card'><div className='flex justify-between'><b>{finding.title}</b><span>{finding.severity}</span></div><p>{finding.target||finding.host||'n/a'} · {finding.tool_id||finding.source}</p><p>{finding.description}</p><pre className='mt-2 overflow-auto text-xs'>{finding.raw_evidence||JSON.stringify(finding.raw_json||finding,null,2)}</pre></div>}

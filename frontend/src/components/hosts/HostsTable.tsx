import type {Host} from '../../lib/api';import {ServicesBadgeList} from './ServicesBadgeList';export function HostsTable({hosts}:{hosts:Host[]}){return <div className='card overflow-auto'><h2 className='mb-3 font-bold'>Hôtes détectés</h2><table className='w-full text-left'><thead><tr className='text-slate-500'><th>IP</th><th>Hostname</th><th>Rôle estimé</th><th>Services</th></tr></thead><tbody>{hosts.map(h=><tr key={h.id} className='border-t border-slate-800'><td className='py-3 font-mono'>{h.ip}</td><td>{h.hostname||'-'}</td><td>{h.is_domain_controller_candidate?'DC candidate':'host'}</td><td><ServicesBadgeList services={h.services}/></td></tr>)}</tbody></table></div>}
export function ParsedResultsPanel({findings=[]}:{findings?:any[]}){return <div className='card'><h2 className='font-bold'>Parsed Results</h2>{findings.length===0?<p className='text-sm text-slate-500'>Parsed findings will appear here after a run.</p>:findings.map((f:any)=><div key={f.id} className='mt-2 rounded border border-slate-700 p-2'><b>{f.title}</b><p>{f.category} · {f.severity} · {f.target}</p><pre className='text-xs'>{JSON.stringify(f.parsed_fields,null,2)}</pre></div>)}</div>}

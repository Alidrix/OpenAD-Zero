import {useState} from 'react';import {uploadBloodHoundZip} from '../../lib/api';
export function UploadDropzone({missionId,onUploaded}:{missionId:string;onUploaded:(c:any)=>void}){const [err,setErr]=useState('');const [busy,setBusy]=useState(false);async function send(f?:File){setErr(''); if(!f)return; if(!f.name.toLowerCase().endsWith('.zip')){setErr('Seuls les fichiers .zip sont acceptés.');return} setBusy(true); try{onUploaded(await uploadBloodHoundZip(missionId,f))}catch(e:any){setErr(String(e.message||e))}finally{setBusy(false)}}return <div className='card'><h2 className='font-bold'>Upload ZIP SharpHound</h2><input type='file' accept='.zip,application/zip' onChange={e=>send(e.target.files?.[0])}/>{busy&&<p>Upload en cours...</p>}{err&&<p className='text-red-400'>{err}</p>}</div>}

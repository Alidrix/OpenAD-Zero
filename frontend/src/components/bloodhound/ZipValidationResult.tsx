export function ZipValidationResult({collection}:{collection:any}){if(!collection)return null;const z=collection.zip_summary_json||{};return <div className='card'><h2 className='font-bold'>Validation ZIP</h2><p>SHA256 : <code>{collection.sha256}</code></p><p>Valide : {String(collection.zip_valid)}</p><p>JSON : {z.json_files_count??'Non disponible'}</p><p>Types : {(z.file_types_detected||[]).join(', ')||'Non disponible'}</p>{(z.warnings||[]).map((w:string)=><p key={w} className='text-amber-400'>{w}</p>)}</div>}

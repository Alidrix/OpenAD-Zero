import {useMemo,useState} from 'react';import {SeverityBadge} from './SeverityBadge';import {EmptyState} from './EmptyState';
export type FindingRow={id?:string;severity?:string;title:string;asset?:string;ip?:string;service?:string;port?:number|string;source?:string;phase?:string;status?:string;created_at?:string;action?:string};
export function FindingTable({findings}:{findings:FindingRow[]}){const [q,setQ]=useState(''),[sev,setSev]=useState(''),[src,setSrc]=useState(''),[phase,setPhase]=useState('');const rows=useMemo(()=>findings.filter(f=>(!q||JSON.stringify(f).toLowerCase().includes(q.toLowerCase()))&&(!sev||f.severity===sev)&&(!src||f.source===src)&&(!phase||f.phase===phase)),[findings,q,sev,src,phase]);return <section className="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm"><div className="flex flex-col gap-3 lg:flex-row lg:items-center lg:justify-between"><h2 className="text-lg font-semibold text-slate-950">Findings</h2><div className="flex flex-wrap gap-2"><input className="rounded-lg border border-slate-200 px-3 py-2 text-sm" placeholder="Search" value={q} onChange={e=>setQ(e.target.value)}/><input className="rounded-lg border border-slate-200 px-3 py-2 text-sm" placeholder="severity" value={sev} onChange={e=>setSev(e.target.value)}/><input className="rounded-lg border border-slate-200 px-3 py-2 text-sm" placeholder="source" value={src} onChange={e=>setSrc(e.target.value)}/><input className="rounded-lg border border-slate-200 px-3 py-2 text-sm" placeholder="phase" value={phase} onChange={e=>setPhase(e.target.value)}/></div></div>{!rows.length?<div className="mt-4"><EmptyState title="No findings detected yet" description="Findings will appear after parsing, discovery, and recompute."/></div>:<div className="mt-4 overflow-x-auto"><table className="min-w-full text-sm"><thead className="text-left text-slate-500"><tr><th className="p-2">Severity</th><th>Title</th><th>Asset/IP</th><th>Service/port</th><th>Source</th><th>Status</th><th>Created</th><th>Action</th></tr></thead><tbody>{rows.map((f,i)=><tr key={f.id||i} className="border-t border-slate-100"><td className="p-2"><SeverityBadge severity={f.severity}/></td><td className="font-medium text-slate-900">{f.title}</td><td>{f.asset||f.ip||'—'}</td><td>{f.service||f.port||'—'}</td><td>{f.source||'—'}</td><td>{f.status||'open'}</td><td>{f.created_at?new Date(f.created_at).toLocaleString():'—'}</td><td>{f.action||'Review'}</td></tr>)}</tbody></table></div>}</section>}

import {motion} from 'motion/react';import {StatusBadge} from './StatusBadge';import type {PentestPhaseDefinition,PentestPhaseState,PentestAction} from '../../lib/v2PentestApi';
export function PhaseTimeline({phases,states,actions}:{phases:PentestPhaseDefinition[];states:PentestPhaseState[];actions:PentestAction[]}){const by=new Map(states.map(s=>[s.phase_id,s]));return <section className="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm"><h2 className="text-lg font-semibold text-slate-950">Pentest phase timeline</h2><div className="mt-5 grid gap-3 lg:grid-cols-2">{phases.sort((a,b)=>a.order-b.order).map((p,i)=>{const st=by.get(p.id);const count=actions.filter(a=>a.phase_id===p.id).length;const done=st?.status==='completed';return <motion.div key={p.id} initial={{opacity:0,x:-8}} animate={{opacity:1,x:0}} transition={{duration:.18,delay:i*.015}} className="relative rounded-xl border border-slate-200 p-4"><div className="flex items-start justify-between gap-3"><div><p className="font-semibold text-slate-900">{p.name}</p><p className="mt-1 text-xs text-slate-500">{p.id}</p></div><StatusBadge status={st?.status||'not_started'}/></div><div className="mt-3 h-1.5 overflow-hidden rounded-full bg-slate-100"><div className={`h-full rounded-full ${done?'w-full bg-emerald-500':st?.status==='running'?'w-2/3 bg-cyan-500':st?.status==='waiting_approval'?'w-1/2 bg-amber-500':'w-1/5 bg-slate-300'}`}/></div><div className="mt-3 flex justify-between text-xs text-slate-500"><span>{st?.summary||p.description}</span><span>{count} actions</span></div></motion.div>})}</div></section>}

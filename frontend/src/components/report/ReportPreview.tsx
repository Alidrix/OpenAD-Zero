import type {ReportPreview as Preview} from '../../types/report';
export function ReportPreview({preview}:{preview:Preview|null}){if(!preview)return <div className="rounded-2xl border border-dashed p-6 text-slate-500">Select a preview format.</div>;return <section className="rounded-2xl border bg-white p-4 dark:border-slate-800 dark:bg-slate-900"><div className="mb-3 flex items-center justify-between"><h2 className="font-semibold">Preview: {preview.format}</h2>{preview.truncated&&<span className="rounded-full bg-amber-100 px-3 py-1 text-xs text-amber-800">truncated</span>}</div>{preview.format==='html'?<iframe title="HTML report preview" sandbox="" srcDoc={preview.content} className="h-[640px] w-full rounded-xl border bg-white"/>:<pre className="max-h-[640px] overflow-auto whitespace-pre-wrap rounded-xl bg-slate-950 p-4 text-sm text-slate-100">{preview.content}</pre>}</section>}

import type {Report} from '../../types/report';
type Props={report:Report|null;loading:boolean;onGenerate:()=>void;onPreview:(format:'markdown'|'html')=>void;downloadMarkdown?:string;downloadHtml?:string};
export function ReportActions({report,loading,onGenerate,onPreview,downloadMarkdown,downloadHtml}:Props){const disabled=!report||loading;return <div className="flex flex-wrap gap-3"><button onClick={onGenerate} disabled={loading} className="rounded-xl bg-blue-600 px-4 py-2 font-semibold text-white disabled:opacity-60">{loading?'Generating…':'Generate report'}</button><button onClick={()=>onPreview('markdown')} disabled={disabled} className="rounded-xl border px-4 py-2 disabled:opacity-50">Preview Markdown</button><button onClick={()=>onPreview('html')} disabled={disabled} className="rounded-xl border px-4 py-2 disabled:opacity-50">Preview HTML</button><a aria-disabled={!report} className={`rounded-xl border px-4 py-2 ${!report?'pointer-events-none opacity-50':''}`} href={downloadMarkdown}>Download Markdown</a><a aria-disabled={!report} className={`rounded-xl border px-4 py-2 ${!report?'pointer-events-none opacity-50':''}`} href={downloadHtml}>Download HTML</a></div>}
